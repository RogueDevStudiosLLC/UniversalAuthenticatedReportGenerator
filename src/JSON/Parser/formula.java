/************************************************
 * Formula Class File							*
 * File name: Formula.java						*
 * The class file for a formula.				*
 ***********************************************/

package JSON.Parser;

/**
 * This class is a formula class.
 * 
 * @author Chel
 * @since 1.0
 */
public class Formula {
	/* Class Attributes */

	/* The name meta of a formula */
	private String _formulaName;
	/* The description of a formula */
	private String _formulaDesc;
	/* The ID of a formula */
	private String _formulaId;
	/* The base equation this formula uses */
	private String _formulaEquation;

	/* Class Methods */
	public void 
	
/**
 * 	Converts variable object values to
 *  variable names compatible with exp4j.
 *  
 * @param Variable	Some variable objects
 * 					to be used in/for the
 * 					expression.
 * 
 */
	private void _exp4jCompatibility (int Variable...) { // Error: Temp ellipses here until fleshed out
		try {
			// Setting variable values to exp4j compatible (temp?) variable names
		} 
		catch (invalidValueException e) {
			// Exit this method/object, cancel export, go back to user input stage
		}
		// Exp4j here or in another method? If here needs to not be void
	}
	
}
